# Test Environment Configuration
NODE_ENV=test
PORT=3000
HOST=localhost

# JWT Configuration
JWT_SECRET=your-test-jwt-secret-key-minimum-32-characters-long
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-test-jwt-refresh-secret-minimum-32-characters-long
JWT_REFRESH_EXPIRES_IN=30d

# Database Configuration (Mocked in tests)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=test_user
DB_PASSWORD=test_password
DB_DATABASE=test_database
DB_SSL_CA_PATH=/path/to/test/ca.pem

# Email Configuration (Mocked in tests)
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASSWORD=test_password
SMTP_FROM_EMAIL=noreply@test.com
SMTP_FROM_NAME=SmashHub Test
